import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  //state用于存储数据
  state: {
    menuList:[
      
    ],
  },
  //mutations用于操作数据（state）
  mutations: {
  },
  //actions用于响应组件中的动作
  actions: {
  },
  modules: {
  }
})
